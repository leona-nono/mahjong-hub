# next-auth@5 beta declares an old nodemailer peer (^6.6.5); we install
# nodemailer@8 for @auth/core compatibility. This keeps `npm install` (Vercel's
# default) from failing on the peer conflict.
legacy-peer-deps=true
