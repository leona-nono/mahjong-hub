# 麻将出海项目 · 网站运营后台设计方案（网页版可视化，基于 FaWeb 范式）

> 更新日期：2026-07-30
> 范式来源：`D:\mahjonggamebox\mahjonggamebox-master`（FaWeb / fafafa.ai 游戏站代码模板，已逆向还原）
> 目标：给运营 / 内容 / 增长团队一个**非技术也能用的网页版可视化后台**，以 FaWeb 的「JSON 配置驱动 + 表单式无代码编辑 + 每语言内容分离」为核心范式，并把项目已有的 Prisma + Auth.js 动态化改造后承载。
> 控费原则：复用 Phase 0 已有的 Auth.js + Prisma/Postgres，**不引入额外 SaaS / 付费 CMS**。

---

## 0. 范式提炼：FaWeb 到底是什么

FaWeb（fafafa.ai）是一个「**无需写代码即可上站**」的一站式出海游戏建站工具。它的代码模板揭示了一个清晰的内容架构范式：

- **配置驱动，而非代码驱动**：整站外观与内容都由 JSON / MDX 配置文件决定，改配置即改站，非技术人员也能运营。
- **每个游戏 = 一套配置包**：`games/<slug>/config.json` + `config/faqs/<lang>.json` + `config/features/<lang>.mdx` + `user-input-text.md`。
- **内容按语言分离**：FAQ 是每语言 JSON 数组，Features 是每语言 MDX 富文本，共 **18 种语言**（en/ja/ko/zh-TW/zh-CN/de/fr/es/ru/pt/pt-BR/it/id/tr/th/vi/uk/bn）。
- **站点级全局配置**：`lib/config/site.json` 管站名、域名、主题、分析 ID、友链、全局下载/显示开关。
- **主题即预设**：`lib/config/themes/*.json`（default / dark / deep-red / deep-sea / cyberpunk-neon）是一组可切换的视觉预设。

**模板实测规模**：游戏目录 **96 个**，每游戏 × 18 语言 = 近 1800 份内容文件。手动改 JSON 不现实 → 必须 DB 化 + 网页表单。

> 结论：我们要建的「运营后台」= **把 FaWeb 这套 JSON 内容模型搬进 Postgres，再用网页表单去读写它**，范式完全一致（表单式、无代码、每语言），但变成动态、可多人协作、可版本化。

---

## 1. FaWeb 数据模型还原（后台要管理的对象）

### 1.1 站点级（`site.json`）
| 字段 | 含义 | 后台表单控件 |
|------|------|------|
| `name` `slogan` `domain` | 站名 / 标语 / 主域名 | 文本框 |
| `icon` `appleIcon` `logoUrl` | 图标 / Logo | 图片上传 |
| `theme.name` | 主题预设 | 下拉（themes/*.json） |
| `gaId` `clarityId` `plausible` | 分析 / 热图 ID | 文本框（SEO 中心联动） |
| `gameType` `gameIframeUrl` `gameRedirectUrl` | 全局游戏模式（iframe / 下载重定向） | 单选 + URL |
| `bgType` `bgImage` `bgVideo` | 首页背景 | 单选 + 媒体上传 |
| `friendLinks[]` | 友链 | 动态列表（名称 + URL） |
| `gameDownload` | 全局下载按钮 + 各平台 URL | 开关 + 多平台 URL |
| `isShowFAQs / isShowVideo / isShowComments / isShowTweets / isShowRecommendation` | 各区块显隐 | 开关组 |
| `comments[]` `tweets[]` `videos[]` | 首页评论 / 推文 / 视频 | 动态列表 |

### 1.2 游戏级（`games/<slug>/config.json`）
| 字段 | 含义 | 后台表单控件 |
|------|------|------|
| `name` `pageName` `pagePath` | 游戏名 / slug / 路径 | 文本 + 自动 slug |
| `categories[]` | 分类（如 "Mahjong Games"） | 标签输入 |
| `gameType` `gameIframeUrl` `gameRedirectUrl` | iframe 地址 / 下载跳转 | 单选 + URL（**核心 SEO 引流入口**） |
| `screenshotUrl` | 游戏截图 | 图片 URL / 上传 |
| `bgType` `bgImage` `bgVideo` | 游戏页背景 | 媒体 |
| `gameDownload` | 单游戏下载链接（覆盖全局） | 多平台 URL |
| `isShowFAQs / isShowVideo / isShowGuide / isShowComments / isShowRecommendation / isShowRightGames / customizeFeatures` | 区块显隐 | 开关组 |
| `videos[]` `comments[]` `tweets[]` | 该游戏视频 / 评论 / 推文 | 动态列表 |
| `status`（`PENDING` 等）`auto_deploy` | 发布状态 / 自动部署 | 状态 + 开关 |

### 1.3 内容块（SEO + GEO 核心，每语言独立）
- **FAQ**：`config/faqs/<lang>.json` = `[{ question, answer }]` 数组 → 后台做**可增删的问答行表单**，渲染时输出 **FAQPage JSON-LD 结构化数据**，直接被 Google / ChatGPT / Perplexity 抓取问答。
- **Features**：`config/features/<lang>.mdx` = 长文富文本（What is / How to play / Key features）→ 后台做**富文本 / Markdown 编辑器**，是 Pillar 长尾内容、GEO 实体覆盖的来源。

### 1.4 i18n 与主题
- `messages/<lang>.json`：UI 文案，按区块（Navbar / HomeFAQs / GamePage / CategoryPage …）。后台做「文案翻译管理」。
- `themes/*.json`：视觉预设，后台做「主题切换 / 自定义色」。

---

## 2. 动态化改造：FaWeb JSON → Postgres（复用 Phase 0）

把上面模型映射成 Prisma 表，后台写 DB，前端经 API 读取。在 `prisma/schema.prisma` 现有表基础上**新增**：

```prisma
model SiteSetting {            // 对应 site.json（单例行）
  id            Int      @id @default(1)
  name          String
  slogan        String
  domain        String
  logoUrl       String?
  themeName     String   @default("default")
  gaId          String?
  clarityId     String?
  plausible     String?
  gameType      String   @default("iframe")
  gameIframeUrl String?
  gameRedirectUrl String?
  bgType        String   @default("iframe")
  bgImage       String?
  bgVideo       String?
  friendLinks   Json?    // [{name,url}]
  gameDownload  Json?    // {showDownloadButton, downloadUrls:{ios,android,...}}
  isShowFAQs    Boolean  @default(true)
  isShowVideo   Boolean  @default(false)
  isShowComments Boolean @default(true)
  isShowRecommendation Boolean @default(true)
  comments      Json?    // 首页评论数组
  tweets        Json?
  videos        Json?
  updatedAt     DateTime @updatedAt
}

model Game {                 // 对应每个 games/<slug>/config.json
  slug              String   @id
  name              String
  pagePath          String
  categories        Json?    // string[]
  theme             Json?    // {headFont,primaryColor,...}
  gameType          String   @default("iframe")
  gameIframeUrl     String?
  gameRedirectUrl   String?
  screenshotUrl     String?
  bgType            String?
  bgImage           String?
  bgVideo           String?
  gameDownload      Json?
  isShowFAQs        Boolean  @default(true)
  isShowVideo       Boolean  @default(false)
  isShowGuide       Boolean  @default(true)
  isShowComments    Boolean  @default(true)
  isShowRecommendation Boolean @default(true)
  isShowRightGames  Boolean  @default(true)
  videos            Json?
  comments          Json?
  tweets            Json?
  status            String   @default("PENDING")
  autoDeploy        Boolean  @default(false)
  sortOrder         Int      @default(0)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  faqs      GameFaq[]
  features  GameFeature[]
}

model GameFaq {              // 对应 faqs/<lang>.json
  id        String   @id @default(cuid())
  game      Game     @relation(fields: [gameSlug], references: [slug], onDelete: Cascade)
  gameSlug  String
  locale    String            // en/ja/ko/zh-TW...
  question  String
  answer    String
  sortOrder Int      @default(0)
  @@unique([gameSlug, locale, question])
}

model GameFeature {          // 对应 features/<lang>.mdx
  id        String   @id @default(cuid())
  game      Game     @relation(fields: [gameSlug], references: [slug], onDelete: Cascade)
  gameSlug  String
  locale    String
  mdx       String            // 富文本内容
  updatedAt DateTime @updatedAt
  @@unique([gameSlug, locale])
}

model ThemePreset {          // 对应 themes/*.json
  id     String @id
  config Json
}

model MessageI18n {          // 对应 messages/<lang>.json
  id     String @id @default(cuid())
  locale String
  section String            // Navbar/GamePage/...
  key    String
  value  String
  @@unique([locale, section, key])
}
```

> 注：`Json` 字段用 Postgres 原生 JSONB（Neon 支持），评论/下载链接等半结构化数据直接存，避免过度范式化。游戏目录 96 个、FAQ/Features 近 1800 行，都在免费额度内。

---

## 3. 后台功能模块（网页版可视化，表单化，无代码）

后台路由区建议放在 **`mahjong-hub/app/admin/*`**（复用 Next 15 + 现有 Auth.js + Prisma，零额外成本）。模块：

### 3.1 站点设置（SiteSetting 表单）
- 站名 / 标语 / 域名 / Logo / 图标（上传）
- 主题预设下拉 + 自定义主色
- 分析 ID（GA / Clarity / Plausible）
- 全局游戏模式（iframe / 下载）+ 默认 iframe 地址
- 首页区块开关（FAQ / 视频 / 评论 / 推荐 / 推文）
- 友链管理（动态列表）

### 3.2 游戏管理（Game CRUD，核心）
- 列表（96 游戏，搜索 / 按分类筛选 / 拖拽排序）
- 新建 / 编辑：名称、slug（自动生成）、分类标签、iframe 地址、下载链接（多平台）、截图、背景、各区块显隐开关、发布状态
- **一键克隆游戏**（快速铺同类游戏）

### 3.3 每游戏 FAQ 编辑器（GameFaq，每语言）
- 顶部语言切换标签（en/ja/ko/zh-TW…）
- 当前语言下「问答行」可增删改、可拖拽排序
- 右侧实时预览「FAQ 结构化数据」JSON-LD 是否符合 schema.org

### 3.4 每游戏 Features 编辑器（GameFeature，每语言）
- 语言切换 + **富文本 / Markdown 编辑器**（支持标题 / 图片 / 卡片网格，对应原 MDX）
- 实时预览渲染效果
- 内置「SEO 评分」：是否含 H2/H3、图片 alt、关键词密度、直接问答段占比

### 3.5 评论管理（comments[]）
- 列表（作者 / 角色 / 头像 / 内容），可审核、编辑、删除
- 支持批量导入（FaWeb 原样用 AI 生成评论文案）

### 3.6 推荐管理（isShowRecommendation）
- 勾选「在首页 / 游戏页右侧展示哪些游戏」

### 3.7 主题 / 外观（ThemePreset）
- 预设切换 + 自定义主色 / 字体 / 背景

### 3.8 i18n 文案（MessageI18n）
- 按区块（Navbar / HomeFAQs / GamePage…）管理多语言 UI 文案，缺译高亮提醒

### 3.9 SEO / GEO 中心（发布前强制校验）
- **可抓取 / 可索引预检**：当前内容是否生成了 `<title>`/`meta description`/`canonical`/`hreflang`，有无 `noindex` 泄漏
- **FAQ 结构化数据校验**：每张 FAQ 是否成功输出 FAQPage JSON-LD（直接影响 GEO 被 AI 引用）
- **直接问答段检测**：Features / FAQ 是否含「What is X / How to play X」式直接答案（GEO 关键）
- **多语言覆盖看板**：96 游戏 × 18 语言 的 FAQ/Features 完成度热力图（暴露内容缺口）
- **排名 / 流量概览**：接 GA / Search Console / Clarity

### 3.10 数据看板（转化漏斗）
- 自然搜索流量、各游戏页 PV、iframe 启动率（游戏即转化）、Shopify 加购 / 结账、积分获取与消耗
- 把"爬虫抓取 → 索引 → 点击 → 玩 → 变现"漏斗可视化

---

## 4. 权限与协作（复用 Phase 0 Auth.js）

- 后台整组 `app/admin/*` 用 `middleware` + `auth()` 保护，仅 `role = ADMIN/OPERATOR` 可进。
- 复用 Phase 0 已建的 `User` 表 + 角色字段；Vercel 团队 Viewer 席位给产品同学看 Preview，后台操作权单独控。
- 角色建议：Admin（全权）/ 运营（内容 + SEO）/ 编辑（仅撰稿）/ 数据（只读看板）。

---

## 5. 技术实现要点

| 项 | 选型 | 说明 |
|----|------|------|
| 框架 | Next 15 App Router（`app/admin/*`） | 与 mahjong-hub 同源，复用布局 / 组件 |
| 表单 | react-hook-form + zod | 类型安全、校验内联 |
| 富文本 | Tiptap / MDX 编辑器 | Features 长文，对应原 MDX |
| 表格 | TanStack Table | 96 游戏列表、排序、筛选 |
| 状态 | Server Actions + Prisma | 表单直写 DB，无需额外 API 层 |
| 鉴权 | 现有 Auth.js + middleware | 零新增依赖 |
| 媒体 | 直传对象存储 / 图床 URL | 截图 / Logo，控制 Image Optimization 用量 |
| 成本 | 仅 Postgres（Neon 免费档） | 无付费 CMS / SaaS |

---

## 6. 与 SEO 框架的衔接（必做项固化进发布流程）

把《SEO 内容建设框架》里"地基层 + GEO 增强层"的要求，**直接做成后台的发布前校验**，而非事后补救：

1. **FAQ 必填 → 自动出 FAQPage JSON-LD**：3.3 编辑器保存即生成结构化数据，3.9 校验通过才能发布。
2. **Features 必含直接问答段**：3.4 编辑器的 SEO 评分强制「What is / How to play」段落。
3. **每语言 hreflang 自动注入**：3.8 i18n 完成度驱动 `<link rel=alternate hreflang>`。
4. **可抓取可索引预检**：3.9 在发布按钮前拦截 `noindex` / 缺 `canonical` / 死链。

> 这正回应用户的核心判断：*「自然流量转化跟爬虫抓取内容完全相关」* —— 后台把"机器可读"做成发布硬门槛。

---

## 7. 阶段排期（并入《开发计划与里程碑》8 月节奏）

| 阶段 | 内容 | 预计 | 依赖 |
|------|------|------|------|
| 后台 P0 | Prisma 新增 6 表 + `app/admin` 骨架 + Auth 保护 | ~6h | Phase 0 DB 就绪 |
| 后台 P1 | 站点设置 + 游戏管理 CRUD（含 FAQ/Features 编辑器） | ~14h | P0 |
| 后台 P2 | i18n 文案 + 主题 + 推荐 + 评论管理 | ~8h | P1 |
| 后台 P3 | SEO/GEO 中心（发布前校验）+ 数据看板 | ~10h | P1 + GA 接入 |
| 内容迁移 | 把 FaWeb 96 游戏配置 / 现有 i18n 导入 DB | ~6h | P1 |

> 优先做 P0+P1（游戏管理 + FAQ/Features 编辑器），因为它直接产出 SEO/GEO 内容，是 8 月底"基础功能 + 商业化跑通"的瓶颈。

---

## 附：FaWeb 模板关键路径速查（供实现参考）

```
lib/config/site.json              站点全局配置
lib/config/themes/*.json          主题预设
app/[locale]/(public)/games/<slug>/config.json        单游戏配置
app/[locale]/(public)/games/<slug>/config/faqs/<lang>.json   每语言 FAQ
app/[locale]/(public)/games/<slug>/config/features/<lang>.mdx 每语言 Features
messages/<lang>.json              UI 文案（Navbar/GamePage/CategoryPage…）
app/[locale]/(public)/games      96 个游戏目录
```
