# 麻将游戏 · 完成度与待办（2026-08-18）

> 分支：`feat/p0-i18n-baseline`（尚未提交）
> 验证基线：`npx tsc --noEmit` → 0 错误；`vitest` → 248 passed / 11 files；5 个 messages JSON 语法全部通过。

---

## 一、今日已完成（本会话改动）

### 1. P0 正确性 / 基建收尾
| 项 | 说明 |
|---|---|
| 港麻文案修正 | `data/games.ts`：牌墙 132→**136 张（含红/绿/白三元）**，与引擎一致；花牌/季节牌按产品规则排除 |
| Prisma 基线迁移 | 新增 `prisma/migrations/20260801000000_init/migration.sql`（17 张表 + 索引 + 外键），修复全新 DB 无法 `migrate deploy` 建表的问题 |
| 根路径 404 修复 | `middleware.ts`：为 `/` 加精确重定向到默认语言（`/en`），开发环境不再 404 |

### 2. 牌桌 UI 国际化（五语：en/ja/ko/zh-TW/zh）
| 组件 | 改动 |
|---|---|
| `MahjongTable.tsx` | 7 处硬编码中文 → next-intl（暂停/继续、牌墙回合、MCR 横幅、补花区、对手花牌计数） |
| `MobileMahjongTable.tsx` | 删除 `isZh` 中英二选一 hack → 真五语 `useTranslations`（约 22 处） |
| `HongKongTable.tsx` | 工具栏、规则/模式/AI 下拉、MCR 教练面板、动作按钮、轮次状态、计分提示弹窗、结算横幅、流局原因（约 40 处） |
| `AmericanMahjongTable.tsx` | 传牌流程、百搭交换、工具栏、步骤条、中央面板、动作按钮、结算复盘、牌谱记录（约 40 处） |

### 3. 新增翻译 key
- `messages/*.json` 的 `mahjong` 命名空间 **+91 key**（暂停/继续、音效、全屏、规则/模式、轮次状态、计分提示、结算横幅、流局原因等）
- `messages/*.json` 新增 `american` 命名空间 **+90 key**（Charleston/传牌、Joker/百搭、Mah Jongg/和牌流程等）

### 4. 过时文案 / 文档勘误
- 港麻「132 张/无白板」文案全部更正为 136 张（游戏内容 + 计分提示弹窗）
- `docs/HONG_KONG_MAHJONG_UPDATE_2026-08-10.md` 顶部加勘误注记
- `docs/RIICHI_WRC_2025_IMPLEMENTATION.md` 的 pao「Deferred」标为已实现

---

## 二、五类四人麻将完成度自查

> 站点「经典麻将」分区共 5 地区（中国含港麻+国标两套，共 6 款）。

### 1. 香港麻将 `hong-kong-mahjong` —— 约 75%
**已完成**
- 136 张牌墙（三元齐全）；三番起胡 + 10 番封顶；Casual（鸡胡）/ Standard 双模式
- 自摸三家各付 2×底、食糊放铳者 4×底；包牌（第三副露/三元刻时点记账）
- 加杠 + 抢杠、海底捞月、杠上开花；番种识别（大/小三元、字一色、十三幺等）
- 独立桌面 + 移动端牌桌、粤语播报、音效、听牌提示

**待完成**
- 计分仍是 `2^fan` 简化公式，非官方「番数-底分」对照表；无「底+番」、连庄加底
- 花牌（144 张）未做（产品决定排除，需规则页声明）
- 两人麻将模式（竞品有 80 张二人港麻）
- 吃碰杠胡动画、固定粤语真人语音（现靠系统 TTS）

### 2. 日本立直麻将 `riichi-mahjong` —— 约 88%（最高）
**已完成**
- 立直/一发/双立直、振听、立直后锁手；表/里/杠宝牌、赤宝牌、`riichiVariant: 'wrc' | 'standard'`
- 符计算、满贯阶梯、役满累计、本场棒、立直棒；WRC pao 责任支付；**双荣 double ron**
- 东南场 8 局 + uma ±15/5、流局听牌罚符；岭上/海底/河底/抢杠/天和/地和/人和、多分解择优
- 标准风味三种途中流局；完整 WRC 役种 + 役满目录

**待完成**
- 双倍役满不区分（国士十三面、四暗刻单骑、纯正九莲按单倍算）
- 流局满贯、飞び/箱下终局、chombo 罚符
- 杠宝牌翻牌时机（暗杠即翻 vs 明杠延后）未建模
- 牌谱回放

### 3. 中国国标麻将 `chinese-official-mahjong`（MCR）—— 约 85%
**已完成**
- 144 张含 8 花、摸花补花 + 后墙补张；8 分起和 + 花分不计入门槛
- 结算 8+F 制（自摸三家各付 8+F；食糊放铳者 8+F、另两家各 8）
- 81 番官方目录（EMA 绿皮书编号 + excludes）；**78/81 番已检测**
- Account-Once 排除引擎 + 多分解择优；花牌区 + 计分教练面板

**待完成**
- 3 个「不靠/组合龙」类番种：Greater Honors and Knitted Tiles(24)、Lesser Honors and Knitted Tiles(12)、Knitted Straight(12)
- 同一手牌多拆分的「不可拆分/不可同组重复」最优选择器未完整实现
- 国标竞赛编排：连庄、盘风、罚则、16 局赛制、比赛排名
- 「训练赛开发中」横幅文案可更新（已 78/81）

### 4. 美国麻将 `american-mahjong` —— 约 55%
**已完成**
- 152 张牌墙、Charleston 三轮 + 第二轮可选 + Courtesy Pass、Joker 换牌
- 原创版本化练习卡（4 张，精确 14 张匹配器）；**全卡线在局 + bot 会碰/杠/宣告**
- 结算、复盘、replay log、Joker 交换面板；UI 国际化（本轮已完成）
- 合规：不实现 NMJL 官方年卡（版权红线）

**待完成**
- NMJL 官方年卡（合规红线，明确不做 → 需当卖点讲）
- NMJL 特定计分/支付例外；进阶对手策略；多回合比赛总账、dead hand/blind pass

### 5 & 6. 四川 / 台湾麻将 —— 引擎 0%（仅可索引落地页）
**已完成**：可索引规则介绍页（intro/howToPlay/tips/faq 完整，五语标题+描述）

**待完成（全部引擎）**
- 四川：108 张牌墙、换三张、定缺、禁吃、血战到底、查大叫/查花猪、刮风下雨、根、番数封顶
- 台湾：16/17 张手牌（需改造 `shanten.ts` 支持 5 面子）、花牌补花/正花、台数加算、连庄累加

---

## 三、跨游戏横向待办（留存 / 流量）

| 缺口 | 现状 |
|---|---|
| 真人对战 | 无 WebSocket；引擎已 UI 无关、为服务端预留 |
| 排行榜 / 战绩 | 无 `/api/leaderboard` |
| `/tools` 算番器/发牌器 | 无路由（复用现成 scoring.ts，ROI 最高） |
| `/learn` 役种/番种词条矩阵 | 无路由（可从 mcr-catalog 程序化生成） |
| 牌谱回放 | 无（seed 确定性引擎，成本最低） |
| 「本站规则 vs 通用规则差异」声明 | 无 |

---

## 四、后端 / 基建待办

- OAuth 凭证（Google/FB/X）、Neon `DATABASE_URL`、`ADMIN_EMAILS` 配置到 Vercel（外部依赖，阻塞线上真实登录/后台闭环）
- blog 纳入 CMS（现为纯静态 `data/blog.ts`）
- 后台 SEO/GEO 发布前校验中心、数据看板、主题管理 UI
- 测试盲区：auth / API / 组件(jsdom) / Prisma 集成 / SEO 快照

---

## 五、当前提交状态

- 分支 `feat/p0-i18n-baseline`，**未提交**
- 待用户本地测试通过后 `git commit` 并推送 `origin`（先 `git ls-remote` 实测凭据）
- 改动文件：5 个 `messages/*.json`、`middleware.ts`、`data/games.ts`、3 个牌桌组件、2 个 docs、1 个新增基线迁移
