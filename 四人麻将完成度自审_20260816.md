# 四人真麻将完成度自审（2026-08-16）

审计对象：`leona-nono/mahjong-hub`（线上 mahjonggame.org）中的五国四人麻将。
参照物：mahjongo.com 竞品调研报告（2026-08-16）。
验证基线：`npm run test` → 202 passed / 11 files；`npx tsc --noEmit` → 无错误。

---

## 〇、一句话结论

**"五国麻将"目前是 3 个能打 + 2 个只有落地页**：日麻做到了准竞技水准（约 75%），港麻可玩但牌墙被砍掉一门（约 65%），国标是"能开局、算不对分"的半成品（约 50%），美式是合规但没有对手的单人练习器（约 35%），四川/台湾**代码量为 0**。

对比 mahjongo：我们**引擎素质更高**（纯函数、可 seed 复现、202 条单测、MCR 81 番目录 + Account-Once 排除引擎，这些 mahjongo 从外部看不出有），但**产品面覆盖差一大截**——竞品 6 种全部可玩、有真人对战、排行榜、算番器/发牌器工具矩阵、`/learn/` 役种词条矩阵，我们这四样**一样都没有**。

---

## 一、逐个游戏的完成度

### 1.1 日本麻将 `riichi-mahjong` —— 完成度 ~75%（最高）

**已实现（相当扎实）**

| 项 | 位置 |
|---|---|
| 立直 / 一发 / 双立直（含"副露前"判定） | `engine.ts:491-498, 639-651` |
| 振听：同巡振听 + 永久振听 | `engine.ts:718-723, 799-802` |
| 立直后摸切锁手 + 暗杠仅在不变听时合法 | `engine.ts:516-533, 634` |
| 表宝牌 / 里宝牌 / 杠后翻宝牌 | `riichi.ts:20-36` |
| 符计算（含荣和明刻 2/4 符、待牌符、七对 25 符、副露平和场 30 符兜底） | `scoring.ts:457-480` |
| 满贯阶梯 + 役满累计 + 本场棒 + 立直棒 | `riichi.ts:42-127` |
| WRC 责任支付 pao（大三元/大四喜/四杠） | `engine.ts:908-928`, `riichi.ts:71-108` |
| 东南场 8 局收官 + 无 oka、±15/5 uma、同分均分 | `engine.ts:307-313, 671-686` |
| 流局听牌罚符 3000 点 | `engine.ts:379-390` |
| 岭上开花 / 海底 / 河底 / 抢杠 / 天和 / 地和 / 人和（5 番非累计） | `scoring.ts:399-438` |
| 多分解择优（先比番，番平比符） | `scoring.ts:524-587` |

**缺口（按影响排序）**

1. **双荣（ダブロン）没有真正实现。** `CLAUDE.md` 明确写"riichi 两家同时荣和、放铳者一人包赔"，但 `maybeResolveClaims()`（`engine.ts:835-842`）在 riichi 下仍然只取一个赢家；已经写好的 `finishWithDoubleRon()`（`engine.ts:939`）**从未被调用，是死代码**，`ResultBanner` 里的 `result.winners` 双荣 UI 也永远不会触发。更要命的是 `tests/mahjong-engine.test.ts:408` 的注释写着"Riichi: every ron winner is paid by the discarder"，断言却是 `winners).toBeUndefined()` —— 注释和断言互相矛盾，**测试在为一个未实现的规则背书**。这正是 `CLAUDE.md` 反复警告的"规则差异共用逻辑"坑，已经第三次出现。
2. **没有赤宝牌（赤5）。** 竞品明确支持"可选赤5"。`VALUES` 表里没有任何 aka 条目，牌墙也没有区分。海外日麻用户（雀魂外溢人群）默认期待有赤。
3. **没有任何途中流局**：九种九牌、四风连打、四杠散了、四家立直、三家和全缺（全库 grep 无 kyuushu/abortive/suufon）。
4. **杠宝牌翻开时机不分明杠/暗杠。** `visibleDoraIndicators()` 只按杠总数即时翻，暗杠即翻、明杠延到打牌后翻的差别没有建模。
5. 无流局满贯、无飞び/箱下终局、无 chombo 罚符。
6. 役满不区分单倍/双倍：国士十三面、四暗刻单骑、纯正九莲都按单倍役满算。
7. 无牌谱回放 —— 竞品 App 已有 replay analysis。

---

### 1.2 香港麻将 `hong-kong-mahjong` —— 完成度 ~65%

**已实现**：3 番起胡 / 10 番封顶（`hongkong.ts:4,30`）、Casual 鸡胡模式（`scoring.ts:413-421`）、自摸三家各付 2×base、食糊放铳者独付 4×base、**包牌**（第四副露或第三只三元刻，在成副露的那一刻记账而非事后推断，`engine.ts:894-905`）、加杠 + 抢杠窗口、海底捞月/杠上开花。

**缺口**

1. **牌墙是 132 张，不是 136 张。** `RULESETS.hongkong.excludedTiles = ['z5']`（`engine.ts:56`），即**整副牌拿掉了白板**。后果不只是少 4 张：
   - `scoring.ts:129` 给港麻 `bigThreeDragons: 8`、`:124` 给 `littleThreeDragons: 5`，但白板不存在 → **大三元/小三元/字一色在港麻永远不可能出现**，这三条番种是纯装饰；
   - `engine.ts:897` 的 `three-open-dragons` 包牌分支同理是死代码；
   - 竞品港麻是标准 136 张（无花、有三元）。**没有任何一个主流港麻平台会去掉白板**，这是我们独有的、且对用户不可解释的偏差。
2. **没有花牌。** 港麻线下 144 张含花，花牌本身是港麻新手最直观的爽点（补花 +1 番、正花翻倍）。
3. 计分是 `2^fan` 的连续公式，不是港麻实际的番数-底分对照表（3 番 8 底、4 番 16 底…），也没有"底 + 番"的底分概念、没有连庄加底。文件自称 "Product v1.0"，属于有意简化，但要写进规则页向用户声明（竞品每页都写"本站规则 vs 通用规则差异"，这点值得抄）。
4. 无两人麻将模式（竞品有 80 张 Golden 分值表的二人港麻）。

---

### 1.3 中国国标 `chinese-official-mahjong` —— 完成度 ~50%

**已实现（架构是三家里最好的）**：144 张含 8 花、摸到花即补花并从后墙补张（`engine.ts:404-414, 429-435`）、8 分起和且**花分不计入起和门槛**（`scoring.ts:668-670`、`engine.ts:749-751`）、`mcr-catalog.ts` 完整 81 番官方目录（含 EMA 绿皮书 chart 编号 + `excludes` 排除关系）、Account-Once 直接排除引擎、多分解按 MCR 结构番择优（`scoring.ts:793-841`）。

**缺口**

1. **81 番只实现了 43 个（53%）。** 用目录 id 与 `MCR_PATTERN_IDS` 映射表比对，未实现的 38 个是：

   `all-fives / all-green / all-terminals / big-four-winds / big-three-winds / chicken-hand / closed-wait / concealed-kong / edge-wait / four-kongs / greater-honors-knitted / knitted-straight / last-tile / last-tile-claim / last-tile-draw / lesser-honors-knitted / little-four-winds / lower-four / melded-hand / melded-kong / mixed-shifted-pungs / mixed-straight / nine-gates / out-with-replacement / outside-hand / pure-terminal-chows / reversible-tiles / robbing-kong / seven-shifted-pairs / single-wait / three-kongs / three-suited-terminal-chows / tile-hog / two-concealed-kongs / two-dragon-pungs / two-melded-kongs / two-terminal-chows / upper-four`

2. **大四喜（88）和小四喜（64）在国标下完全检测不到。** 风刻检测块被 `if (ruleset === 'riichi' || ruleset === 'hongkong')` 包住（`scoring.ts:295-311`），国标走不进去。玩家做出 88 分限定番会得 0。
3. **结算公式不符合国标。** `engine.ts:1027-1036`：自摸时每家付 `F`，点炮时只有点炮者付 `F`。国标标准是**自摸每家付 8+F；点炮者付 8+F，另外两家各付 8**。目前既漏了 8 分底，也漏了闲家的底分支付 —— 分数账目对不上任何一本国标记分表。
4. 边张/坎张/单钓/自摸/杠上/抢杠这类"和牌方式番"在国标下几乎全缺（见上表 `edge-wait` 等），而这些是国标凑够 8 分的常用手段，缺了会让"起和门槛 8 分"体感虚高。
5. UI 自己也承认："MCR 训练赛开发中 …… 完整 81 番计分和国标专属结算正在实现"（`MahjongTable.tsx` MCR 横幅）。诚实，但这条横幅挂在一个已经 `native` + 进 sitemap + 被搜索引擎索引的页面上。

---

### 1.4 美国麻将 `american-mahjong` —— 完成度 ~35%（合规姿势正确）

**已实现**：152 张牌墙（136 + 8 花 + 8 Joker，`american.ts:8`）、Charleston 三轮传牌 + 第二轮 Charleston 可选 + Courtesy Pass（`american.ts:82-101`）、Joker 换牌（`:147-157`）、认牌优先级函数（`:125-128`）、7 张**自研原创练习卡**（`ORIGINAL_PRACTICE_CARDS`，带 id/version，明确"Release a new id/version instead of mutating a released card"）。

**合规红线守得很好**：完全没有 NMJL 牌型卡素材，`CLAUDE.md` 的"不实现美式 NMJL 牌型"被严格执行。竞品 mahjongo 直接实现 NMJL 卡牌型（其页面自己声明"须以当年官方卡为准"），法律风险高于我们——**这是我们的优势，不是缺陷，但要在产品文案上把"原创练习卡"讲成卖点而不是缺失**。

**缺口**

1. **三个 AI 对手不会赢。** `legalAmericanClaims` / `claimAmericanMahJong` / `declareAmericanMahJong` 的 `seat` 默认值全是 `0`，UI 里也只对人类座位调用（`AmericanMahjongTable.tsx:93,160`）。bot 只会摸一张打一张，**从不碰、不杠、不宣告 Mah Jongg**。这不是"四人对战"，是"三个陪打机器人"。
2. **一局只锁定一条卡线。** `createAmericanGame(seed, cardId)` 固定一个目标牌型，玩家开局就知道要凑什么。真实美式的核心乐趣恰恰是"手上这把牌能走卡上哪几条线"的搜索与切换——目前这层策略完全不存在。
3. 庄家不发 14 张（四家均 13 张，`american.ts:46`），首张牌在 Charleston 结束后才摸。
4. 回合流转是硬编码的："人打 → 座位1 打 → 开认牌窗 → 座位2、3 补打"（`:164-193`），不是真正的轮转。
5. 无牌墙耗尽的 wall game、无 dead hand、无 blind pass。
6. **UI 零国际化**：`AmericanMahjongTable.tsx` 里 `useTranslations` 调用数为 0，全部硬编码英文。

---

### 1.5 四川麻将 `sichuan-mahjong` —— 完成度 0%（仅落地页）
### 1.6 台湾麻将 `taiwan-mahjong` —— 完成度 0%（仅落地页）

两者都是 `gameType: 'coming-soon'`，`data/games.ts` 里有写得相当好的 intro / howToPlay / tips / faq，`data/games.i18n.ts` 里有 zh/zh-TW/ja/ko 的标题+描述翻译（但没有 content 翻译，靠逐词回退英文）。引擎侧**没有任何一行四川/台湾代码**：

- 四川需要的：108 张三门牌墙、换三张、定缺、禁吃、血战到底（多赢家）、查大叫/查花猪、刮风下雨、根、番数封顶 —— 全部为空。当前 `Ruleset` 联合类型只有 `'hongkong' | 'riichi' | 'chinese-official'`。
- 台湾需要的：16/17 张手牌、5 面子 + 1 雀头（`shanten` 现在硬编码 4 面子结构）、花牌补花与正花、台数加算制、连庄累加 —— 全部为空。**16 张会动到 shanten 核心**，是这两个里工作量更大的一个。

**并且这两页现在是 `noindex`**：`app/[locale]/(public)/games/[slug]/page.tsx:52` 只对 `gameType === 'native'` 发 `index: true`，coming-soon 落到 `index: false`；`app/sitemap.ts` 也只收 native。这**违背了 `CLAUDE.md` 里"由 `ComingSoonGame.tsx` 渲染可索引介绍页"的约定**，等于把已经写好的四川/台湾长尾内容主动藏起来——而竞品 `/sichuan`、`/taiwan` 两页是实打实在吃这部分自然流量的（其台湾流量占比 2.66%）。顺带：`ComingSoonGame.tsx` 有编码损坏，`??` 和 `?` 是乱码的 emoji 与破折号。

---

## 二、跨游戏的横向缺口（比单个游戏的规则漏洞更致命）

| 缺口 | 现状 | 竞品对照 |
|---|---|---|
| **真人对战** | 完全没有。全库 grep 无 WebSocket/socket.io 实现，`BACKEND.md` 只有规划 | mahjongo 有匹配、金币场、好友对局 |
| **排行榜 / 战绩** | 没有。`NativeGameMount` 明确注释"浏览器结果不得铸造账号价值，points 留给未来服务端结算回执" —— 架构选择正确，但产品上等于零留存钩子 | 每玩法独立排行榜 + 总局/胜/负 |
| **算番器 / 发牌器工具页** | 没有 `/tools` 路由（find 无结果） | 7 个工具页，是其最高价值长尾入口 |
| **役种 / 番种词条矩阵** | 没有 `/learn` 路由 | `/learn/yakulist/*` 每个役种独立词条 |
| **牌谱回放** | 没有。但引擎是 seed 确定性纯函数，**回放的地基已经打好了**，这是最低成本的差异化功能 | App 有 replay analysis |
| **游戏内 UI 多语言** | `messages/*.json` 五语 key 完全对齐（186 key，其中 mahjong 24 个），但**牌桌组件几乎不用它**：`HongKongTable` 2 处 `useTranslations`，`MobileMahjongTable` 0 处，`AmericanMahjongTable` 0 处；`MahjongTable` 里还混着硬编码中文（"MCR 训练赛开发中"、"牌墙"、"回合"、"补花区"、"▶ 继续"）。**五语站点的核心产品是中英混杂的** | 全站多语言，日文站换品牌名"麻雀國" |
| **规则透明声明** | 游戏页有 content 文案，但没有像竞品那样逐条写"本站规则 vs 通用规则的差异" | 每玩法页都有，既建立专业信任又吃 SEO |

---

## 三、待开发功能清单（按 ROI 排序）

### P0 — 修正确性 / 违背既有约定的问题（本周，工作量都很小）

| # | 任务 | 位置 | 说明 |
|---|---|---|---|
| 1 | 国标结算改成 `8+F` 制 | `engine.ts:1027-1036` | 自摸每家付 8+F；点炮者付 8+F，另两家各付 8。当前账目对不上任何国标记分表 |
| 2 | 国标补大四喜 / 小四喜检测 | `scoring.ts:295-311` | 去掉 `ruleset === 'riichi' \|\| 'hongkong'` 的门禁，按 ruleset 取值。**按 CLAUDE.md 要求为国标和港麻各补一条测试** |
| 3 | 日麻双荣：接上 `finishWithDoubleRon` 或明确降级为头跳 | `engine.ts:835-842, 939` | 二选一，但必须让代码、`CLAUDE.md`、`tests/mahjong-engine.test.ts:408` 的注释三者一致。目前测试注释在为未实现的行为背书 |
| 4 | 港麻牌墙决策：恢复白板（→136 张），或删掉三元番种 + 包牌死分支并在规则页声明 | `engine.ts:56`, `scoring.ts:124,129`, `engine.ts:897` | 建议**恢复白板**。去掉白板没有任何用户可解释的理由，还白白废掉一整族番种 |
| 5 | 四川/台湾落地页改为可索引并进 sitemap | `games/[slug]/page.tsx:52`, `app/sitemap.ts:68` | 按 `CLAUDE.md` 的原意执行；顺手修 `ComingSoonGame.tsx` 的乱码 |

### P1 — 补齐"能玩且算得对"（1–2 个月）

| # | 任务 | 预估 |
|---|---|---|
| 6 | 国标 81 番补到 70+：优先补和牌方式番（边张/坎张/单钓/杠上开花/抢杠/妙手回春/海底捞月）和门风类，这些是凑够 8 分的主力 | 大 |
| 7 | 美式：让 bot 会碰/杠/宣告 Mah Jongg，改成真正的四家轮转 | 中 |
| 8 | 美式：一局同时开放多条卡线，玩家可中途切换目标 —— 这是美式玩法的灵魂 | 中 |
| 9 | 日麻：赤宝牌（可开关）+ 九种九牌/四风连打/四杠散了三种途中流局 | 中 |
| 10 | 港麻：加回花牌与补花、正花判定 | 中 |
| 11 | **牌桌 UI 全面接入 next-intl**，清掉所有硬编码中英文；`messages/` 五语同步补 key | 中 |
| 12 | 每个游戏页加"本站规则 vs 通用规则差异"板块（抄竞品结构，SEO + 信任双收） | 小 |

### P2 — 新玩法与流量资产（2–4 个月）

| # | 任务 | 预估 |
|---|---|---|
| 13 | **四川麻将**：108 张牌墙、换三张、定缺、禁吃、血战到底多赢家、查大叫/查花猪、根与番数封顶。不动 shanten 核心，是两者中更快的一个 | 大 |
| 14 | **台湾麻将**：16/17 张手牌需要**改造 `shanten.ts` 支持 5 面子结构**，加花牌补花与正花、台数加算、连庄累加 | 很大 |
| 15 | **算番器 / 发牌器工具矩阵** `/tools/*`：日麻算点器、国标算番器、台湾算台器、牌型生成器（可导出图片分享）。竞品最高价值长尾入口，且**纯前端、复用现成 `scoring.ts`，投入产出比最高** | 中 |
| 16 | **役种/番种词条矩阵** `/learn/*`：81 番 + 日麻役种各一页，五语。内容可从 `mcr-catalog.ts` 与 `VALUES` 表程序化生成 | 中 |
| 17 | **牌谱回放**：引擎已是 seed 确定性纯函数，只需持久化 `{seed, ruleset, actions[]}` 即可完整重放。低成本高感知，竞品才刚有 | 小 |

### P3 — 留存与变现（DAU 起来之后再做）

| # | 任务 |
|---|---|
| 18 | 服务端权威的多人对战：引擎已刻意保持 UI 无关且可跑在 Node（`engine.ts` 文件头注释），这条路径的技术债最小 |
| 19 | 账号绑定的战绩与排行榜（分 ruleset），配合服务端结算回执发放积分 |
| 20 | 分阶段变现：休闲消除页先跑 AdSense/激励视频，真麻将 DAU 起来后再上金币/去广告订阅 |

---

## 四、给决策者的三条判断

1. **不要急着上四川和台湾。** 现在把"五国麻将"当卖点宣传，实际是 3 个能打 + 2 个空壳，其中还有 1 个（国标）算分不对。**先把 P0 的 5 条修掉，再把国标补到能正确结算**，比多上两个国家的半成品更能守住"规则严谨"这个我们真正能赢竞品的点。

2. **工具页和词条矩阵应该插队到新玩法前面。** `/tools` + `/learn` 完全复用已有的 `scoring.ts` 和 `mcr-catalog.ts`，纯前端、不碰引擎、不引入规则风险，却是竞品被验证过的最高价值自然流量入口。四川/台湾引擎则是纯投入、且台湾要动 shanten 核心。

3. **美式的"原创练习卡"要当卖点讲，不要当缺陷藏。** 竞品直接实现 NMJL 牌型且自己声明"须以当年官方卡为准"，法律暴露面比我们大。我们的路线是对的，缺的只是"让这套原创卡玩起来像真的麻将"——即 P1 的第 7、8 条（会赢的 bot + 多卡线搜索）。补完这两条，美式就是一个既合规又好玩的差异化产品，面向的还是竞品报告里那个"35 万会员、付费意愿高、线上竞品少"的人群。
